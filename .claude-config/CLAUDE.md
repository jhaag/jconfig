# User-level Claude Code Memory

Personal defaults and preferences for Claude Code across all projects.

## Preferences

- Prefer concise responses
- Use existing code patterns when modifying projects
- Never push to remote repositories without explicit approval
